services:
  - type: web
    name: merkipaki
    env: docker
    plan: free
    dockerfilePath: Dockerfile
    envVars:
      - key: JWT_SECRET
        sync: false
      - key: APIFY_TOKEN
        sync: false
